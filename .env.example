# Copy to .env and fill in. .env is gitignored and must NEVER be committed.

# Optional: Dispatcharr exporter/source
PROBARR_DISPATCHARR_URL=http://dispatcharr:9191
PROBARR_DISPATCHARR_USER=
PROBARR_DISPATCHARR_PASS=

# Optional: default playlist source (file path or http(s) URL)
PROBARR_SOURCE=
